<!DOCTYPE sqlMap
    PUBLIC "-//iBATIS.com//DTD SQL Map 2.0//EN"
    "http://ibatis.apache.org/dtd/sql-map-2.dtd">
<sqlMap namespace="Comment">
	<typeAlias alias="Comment" type="kr.co.webcash.domain.Comment"/>
		
	<insert id="insert" parameterClass="Comment">
		insert into comment (id, targetId, commentType, displayId, writerId,contents, dateCreated) 
		values(#id#, #targetId#, #type#, #displayId#, #writer.loginId#, #contents#, #dateCreated#)
	</insert>
	
	<update id="update" parameterClass="Comment">
		update comment set contents = #contents#
		where id = #id#
	</update>
	
	<delete id="delete" parameterClass="Comment">
		delete from Comment
		where id = #id#
	</delete>
	
	
	<sql id="selectColumn">
		c.id as id,
		c.targetId as targetId,
		c.commentType as type,
		c.displayId as displayId,
		c.writerId as "writer.loginId",
		c.contents as contents,
		c.dateCreated as dateCreated
	</sql>
	
	<select id="findAllByTargetIdAndType" resultClass="Comment">
		SELECT
			<include refid="selectColumn" />
		FROM comment c
		where c.targetId = #targetId# and
			c.commentType = #type#
		order by c.id desc
	</select>
	
	<select id="findByDisplayIdAndTargetIdAndType" resultClass="Comment">
		SELECT
			<include refid="selectColumn" />
		FROM comment c
		where c.displayId = #displayId# and 
			c.targetId = #targetId# and
			c.commentType = #type#
	</select>
	
	<select id="findLastByBlogIdAndCommentType" resultClass="Comment">
		SELECT
			<include refid="selectColumn" />
		FROM comment c
		join post p on p.id = c.targetId
		join category ca on ca.id = p.categoryId
		join blogs b on b.id = ca.blogId
		where ca.blogId = #blogId# and
			c.commentType = #type#
		order by c.id desc
		limit 1
	</select>
	
	<select id="findLast" resultClass="Comment">
		SELECT
			<include refid="selectColumn" />
		FROM comment c
		join post p on p.id = c.targetId
		join category ca on ca.id = p.categoryId
		join blogs b on b.id = ca.blogId
		order by c.id desc
		limit 1
	</select>
</sqlMap>