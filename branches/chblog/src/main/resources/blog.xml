<!DOCTYPE sqlMap
    PUBLIC "-//iBATIS.com//DTD SQL Map 2.0//EN"
    "http://ibatis.apache.org/dtd/sql-map-2.dtd">
<sqlMap namespace="Blog">
	<typeAlias alias="Blog" type="kr.co.webcash.domain.Blog"/>
		
	<insert id="create" parameterClass="Blog">
		insert into blogs (id, title, owner,dateCreated) 
		values(#id#, #title#, #owner#, #dateCreated#)
	</insert>
	
	<update id="update" parameterClass="Blog">
		update blogs set
			title = #title#
		where id = #id#
	</update>
	
	<select id="findByUserLoginId" resultClass="Blog">
		SELECT id, title, owner, dateCreated
		FROM blogs
		WHERE owner = #loginId#
	</select>
	
	<select id="findById" resultClass="Blog">
		SELECT id, title, owner, dateCreated
		FROM blogs
		WHERE id = #id#
	</select>
</sqlMap>