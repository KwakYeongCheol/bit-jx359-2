<!DOCTYPE sqlMap
    PUBLIC "-//iBATIS.com//DTD SQL Map 2.0//EN"
    "http://ibatis.apache.org/dtd/sql-map-2.dtd">
<sqlMap namespace="Trackback">
	<typeAlias alias="Trackback" type="kr.co.webcash.domain.Trackback"/>
		
	<insert id="insert" parameterClass="Trackback">
		insert into trackback (blogId, postId, url, blogName, title, excerpt, dateCreated) 
		values(#blogId#, #postId#, #url#, #blogName#, #title#, #excerpt#, #dateCreated#)
	</insert>

	<select id="findAllByBlogIdAndPostId" resultClass="Trackback">
		SELECT
			blogId, postId, url, blogName, title, excerpt, dateCreated
		FROM trackback
		where blogId = #blogId# and postId = #postId#
	</select>
</sqlMap>