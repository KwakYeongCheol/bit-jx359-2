<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Category">
	<insert id="insert" parameterType="Category">
		insert into category (id, title, blogId, displayId) 
		values(#{id}, #{title},  #{blog.id}, #{displayId})
	</insert>
	
	<update id="update" parameterType="Category">
		update category set title = #{title}
		where id = #{id}
	</update>
	
	<delete id="delete" parameterType="Category">
		delete from category
		where id = #{id}
	</delete>
	
	<sql id="select">
		SELECT
			c.id as id, 
			c.blogId as "blog.id", 
			c.displayId as displayId,
			c.title as title
		FROM category c
	</sql>
	
	<select id="findAllByBlogId" resultType="Category">
		<include refid="select"/>
		WHERE
			c.blogId = #{blogId}
		ORDER BY c.id DESC
	</select>
	
	<select id="findLast" resultType="Category">
		<include refid="select"/>
		ORDER BY c.id DESC
		LIMIT 1
	</select>
	
	<select id="findLastPostByBlogId" resultType="Category">
		<include refid="select"/>
		WHERE
			c.blogId = #{blogId}
		ORDER BY c.id DESC
		LIMIT 1
	</select>
	
	<select id="findByBlogIdAndDisplayId" resultType="Category">
		<include refid="select"/>
		WHERE
			c.blogId = #{blogId}
			AND c.displayId = #{displayId}
	</select>
	
	<select id="findAllCountByIdAndBlogId" resultType="Long">
		SELECT
			count(*)
		FROM
			category
		WHERE
			blogId = #{blogId}
	</select>
</mapper>