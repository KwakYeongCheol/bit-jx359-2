<!DOCTYPE sqlMap
    PUBLIC "-//iBATIS.com//DTD SQL Map 2.0//EN"
    "http://ibatis.apache.org/dtd/sql-map-2.dtd">
<sqlMap namespace="Guestbook">
	<typeAlias alias="Guestbook" type="kr.co.webcash.domain.Guestbook"/>
		
	<insert id="insert" parameterClass="Guestbook">
		insert into guestbook (id, blogId, displayId, writer, contents, dateCreated) 
		values(#id#, #blog.id#, #displayId#, #writer#, #contents#, #dateCreated#)
	</insert>
	
	<update id="update" parameterClass="Guestbook">
		update guestbook set contents = #contents#
		where id = #id#
	</update>
	
	<delete id="delete" parameterClass="Guestbook">
		delete from guestbook
		where id = #id#
	</delete>
	
	<select id="findLastGuestbookByBlogId" resultClass="Guestbook">
		select id
		from guestbook
		where blogId = #blogId#
		order by id desc
		limit 1
	</select>
	
	<sql id="defaultSelect">
		g.id as id, 
		g.displayId as displayId,
		g.writer as writer,
		g.contents as contents, 
		g.dateCreated as dateCreated
	</sql>
	
	<select id="findAllByBlogId" resultClass="Guestbook">
		SELECT 
			<include refid="defaultSelect"/>
		FROM guestbook g
		where g.blogId = #blogId#
		order by g.id desc
	</select>
	
	<select id="findByBlogIdAndDisplayId" resultClass="Guestbook">
		SELECT 
			<include refid="defaultSelect"/>
		FROM guestbook g
		join blogs b on b.id = g.blogId
		where g.blogId = #blogId#
			and g.displayId = #displayId#
	</select>
	
	<select id="findLast" resultClass="Guestbook">
		SELECT
			<include refid="defaultSelect"/>
		FROM guestbook g
		order by g.id desc
		limit 1
	</select>
	
	
</sqlMap>